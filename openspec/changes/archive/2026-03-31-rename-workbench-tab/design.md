## Context

底部导航栏使用 go_router 的 ShellRoute 配置，标签定义在 `home_shell.dart` 中。

## Goals / Non-Goals

**Goals:**
- 将"工作台"改为"遥控器"

**Non-Goals:**
- 不修改页面逻辑或路由结构
- 不修改其他UI元素

## Decisions

直接修改 `home_shell.dart` 中的标签文本即可完成。

## Risks / Trade-offs

无风险。